# Static keyword 
In zura we use the keyword `static` to define an immutable variable. The `static` keyword is followed by the variable name and the `:=` operator and then the value of the variable. 

```zura
static name := "Zura";
```
